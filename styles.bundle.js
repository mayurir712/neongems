webpackJsonp(["styles"],{

/***/ "./node_modules/raw-loader/index.js!./node_modules/postcss-loader/lib/index.js??embedded!./node_modules/sass-loader/lib/loader.js??ref--8-3!./src/assets/scss/styles.scss":
/***/ (function(module, exports) {

module.exports = "/* You can add global styles to this file, and also import other style files */\nhtml, body {\n  height: 100%; }\nul {\n  margin: 0;\n  padding: 0;\n  list-style: none; }\na:focus, a:hover {\n  text-decoration: none; }\nbody {\n  margin: 0;\n  font-family: 'Open Sans', sans-serif; }\n.nopadding {\n  padding: 0; }\n.btm_padding {\n  padding-bottom: 30px; }\n.container_custom {\n  width: 100%;\n  height: 100%; }\n.container_custom .home_page {\n    width: 100%;\n    display: -webkit-box;\n    display: -ms-flexbox;\n    display: flex;\n    height: 100%; }\n.container_custom .home_page .menu {\n      background: black;\n      -webkit-transition: 2s;\n      transition: 2s;\n      width: 5%;\n      display: -webkit-box;\n      display: -ms-flexbox;\n      display: flex;\n      -webkit-box-pack: center;\n          -ms-flex-pack: center;\n              justify-content: center;\n      -webkit-box-orient: vertical;\n      -webkit-box-direction: normal;\n          -ms-flex-direction: column;\n              flex-direction: column;\n      text-align: center;\n      float: left; }\n.container_custom .home_page .menu .sidenav {\n        text-transform: uppercase;\n        height: 100%;\n        /* 100% Full-height */\n        width: 0;\n        /* 0 width - change this with JavaScript */\n        position: fixed;\n        /* Stay in place */\n        z-index: 1;\n        /* Stay on top */\n        top: 0;\n        /* Stay at the top */\n        left: 0;\n        background-color: white;\n        /* Black*/\n        overflow-x: hidden;\n        /* Disable horizontal scroll */\n        -webkit-transition: 0.5s;\n        transition: 0.5s;\n        /* 0.5 second transition effect to slide in the sidenav */\n        display: -webkit-box;\n        display: -ms-flexbox;\n        display: flex;\n        -webkit-box-pack: center;\n            -ms-flex-pack: center;\n                justify-content: center;\n        -webkit-box-orient: vertical;\n        -webkit-box-direction: normal;\n            -ms-flex-direction: column;\n                flex-direction: column;\n        text-align: center; }\n.container_custom .home_page .menu .sidenav ul li a {\n          padding: 8px 8px 8px 8px;\n          text-decoration: none;\n          font-size: 14px;\n          color: #818181;\n          display: inline-block;\n          -webkit-transition: 0.3s;\n          transition: 0.3s; }\n.container_custom .home_page .menu .sidenav ul li a.active {\n          color: #000; }\n.container_custom .home_page .menu .sidenav ul.nav {\n          margin: 40px 0; }\n.container_custom .home_page .menu .sidenav ul.nav li:hover a {\n            color: black;\n            background: none; }\n.container_custom .home_page .menu .sidenav ul.social {\n          display: -webkit-box;\n          display: -ms-flexbox;\n          display: flex;\n          -webkit-box-pack: center;\n              -ms-flex-pack: center;\n                  justify-content: center;\n          margin-bottom: 40px; }\n.container_custom .home_page .menu .sidenav ul.social li {\n            margin: 0 5px; }\n.container_custom .home_page .menu .sidenav ul.social li a {\n              width: 25px;\n              height: 25px;\n              font-size: 13px;\n              border-radius: 50%;\n              background: black;\n              color: white;\n              line-height: 10px; }\n.container_custom .home_page .menu .sidenav .closebtn {\n          position: absolute;\n          top: 50%;\n          right: 25px;\n          font-size: 36px;\n          margin-left: 50px;\n          margin-top: -25px;\n          z-index: 9;\n          text-decoration: none;\n          color: black; }\n.container_custom .home_page .menu .click {\n        text-transform: uppercase;\n        font-size: 11px;\n        cursor: pointer;\n        color: white;\n        margin: 10px 0; }\n.container_custom .home_page .menu .click div {\n          width: 25px;\n          height: 2px;\n          background-color: white;\n          margin: 0 auto;\n          margin-bottom: 6px; }\n.container_custom .home_page .menu #copyright {\n        font-style: italic;\n        color: white; }\n.container_custom .home_page .menu:hover {\n      background: white; }\n.container_custom .home_page .menu:hover .click {\n        color: black; }\n.container_custom .home_page .menu:hover .click div {\n          background: black; }\n.container_custom .home_page #main {\n      width: 95%;\n      display: -webkit-box;\n      display: -ms-flexbox;\n      display: flex;\n      -webkit-transition: margin-left .5s;\n      transition: margin-left .5s; }\n.container_custom .home_page #main .wide {\n        width: 100%;\n        display: -webkit-box;\n        display: -ms-flexbox;\n        display: flex; }\n.container_custom .home_page #main .wide .banner {\n          width: 28%;\n          color: white;\n          background: black;\n          /*background: url('../images/lady.png'); background-size: 100% 100%;  */ }\n.container_custom .home_page #main .wide .banner .overlay {\n            background: rgba(0, 0, 0, 0.5);\n            width: 100%;\n            height: 100%;\n            display: -webkit-box;\n            display: -ms-flexbox;\n            display: flex;\n            -webkit-box-pack: center;\n                -ms-flex-pack: center;\n                    justify-content: center;\n            -webkit-box-orient: vertical;\n            -webkit-box-direction: normal;\n                -ms-flex-direction: column;\n                    flex-direction: column;\n            text-align: center; }\n.container_custom .home_page #main .wide .banner .wrap_ban_data {\n            margin: 0 30px; }\n.container_custom .home_page #main .wide .banner .wrap_ban_data img {\n              margin: 15px 0; }\n.container_custom .home_page #main .wide .banner .wrap_ban_data h1 {\n              font-size: 36px;\n              line-height: 46px;\n              text-transform: capitalize;\n              margin: 0; }\n.container_custom .home_page #main .wide .banner .wrap_ban_data span {\n              display: block;\n              text-transform: uppercase;\n              padding-top: 15px;\n              padding-bottom: 15px; }\n.container_custom .home_page #main .wide .banner .wrap_ban_data ul {\n              display: -webkit-box;\n              display: -ms-flexbox;\n              display: flex;\n              list-style: none;\n              -webkit-box-pack: center;\n                  -ms-flex-pack: center;\n                      justify-content: center;\n              border-top: 1px solid #fff;\n              padding: 10px 0; }\n.container_custom .home_page #main .wide .banner .wrap_ban_data ul li a {\n                margin: 0 8px;\n                display: block;\n                color: #fff; }\n.container_custom .home_page #main .wide .title {\n          margin-bottom: 30px;\n          text-transform: capitalize;\n          text-align: center; }\n.container_custom .home_page #main .wide .title span {\n            display: inline-block;\n            position: relative; }\n.container_custom .home_page #main .wide .title span:after {\n            overflow: visible;\n            /* For IE */\n            padding: 0;\n            position: absolute;\n            content: \"\";\n            left: 0;\n            border: none;\n            border-top: medium double #333;\n            color: #333;\n            text-align: center;\n            bottom: -10px;\n            width: 100%; }\n.container_custom .home_page #main .wide .gallery {\n          width: 72%;\n          overflow: auto; }\n.container_custom .home_page #main .wide .gallery .sticky {\n            margin-bottom: -50px;\n            min-height: 100%; }\n.container_custom .home_page #main .wide .gallery .sticky .Card {\n              margin: 15px;\n              text-align: center; }\n.container_custom .home_page #main .wide .gallery .sticky .Card h1 {\n                font-family: 'Playfair Display', serif; }\n.container_custom .home_page #main .wide .gallery .sticky .Card img {\n                width: 100%;\n                height: auto; }\n.container_custom .home_page #main .wide .gallery .sticky .Card .img-thumbnail {\n                padding: 15px; }\n.container_custom .home_page #main .wide .gallery .sticky .grid {\n              text-align: center; }\n.container_custom .home_page #main .wide .gallery .sticky .grid .grid-item {\n                width: 50%;\n                -webkit-transition: width 2s;\n                transition: width 2s; }\n.container_custom .home_page #main .wide .gallery .sticky .grid .grid-item .wrap {\n                  position: relative; }\n.container_custom .home_page #main .wide .gallery .sticky .grid .grid-item .wrap img {\n                    opacity: 1;\n                    -webkit-transition: all 0.3s;\n                    transition: all 0.3s;\n                    -webkit-backface-visibility: hidden;\n                            backface-visibility: hidden;\n                    width: 100%;\n                    height: auto;\n                    display: block; }\n.container_custom .home_page #main .wide .gallery .sticky .grid .grid-item .wrap .pos {\n                    display: none;\n                    position: absolute;\n                    left: 0;\n                    top: 0;\n                    width: 100%;\n                    height: 100%;\n                    z-index: 0; }\n.container_custom .home_page #main .wide .gallery .sticky .grid .grid-item .wrap .pos a {\n                      width: 100%;\n                      height: 100%;\n                      -webkit-box-pack: center;\n                          -ms-flex-pack: center;\n                              justify-content: center;\n                      -webkit-box-orient: vertical;\n                      -webkit-box-direction: normal;\n                          -ms-flex-direction: column;\n                              flex-direction: column;\n                      display: -webkit-box;\n                      display: -ms-flexbox;\n                      display: flex; }\n.container_custom .home_page #main .wide .gallery .sticky .grid .grid-item .wrap .pos a h3 {\n                        position: relative;\n                        z-index: 9;\n                        color: black;\n                        text-transform: uppercase; }\n.container_custom .home_page #main .wide .gallery .sticky .grid .wrap:hover .pos {\n                -webkit-box-pack: center;\n                    -ms-flex-pack: center;\n                        justify-content: center;\n                -webkit-box-orient: vertical;\n                -webkit-box-direction: normal;\n                    -ms-flex-direction: column;\n                        flex-direction: column;\n                text-align: center;\n                display: -webkit-box;\n                display: -ms-flexbox;\n                display: flex; }\n.container_custom .home_page #main .wide .gallery .sticky .grid .wrap:hover img {\n                opacity: 0.3;\n                /*-moz-transform: scale(1.1);\n                        -webkit-transform: scale(1.1);\n                        transform: scale(1.1);*/ }\n.container_custom .home_page #main .wide .gallery .grid-item--width2 {\n            width: 100%;\n            height: auto; }\n.container_custom .home_page #main .wide .gallery .footer {\n            background: black;\n            color: #fff;\n            height: 50px;\n            /*.footer,.push {  height: 50px;}*/ }\n.container_custom .home_page #main .wide .gallery .footer ul li > a {\n              color: White;\n              text-transform: capitalize;\n              margin: 0 15px;\n              padding: 15px 0; }\n.container_custom .home_page #main .wide .gallery .footer ul li > a span.caret {\n                border-bottom: 4px dashed;\n                border-top: 0; }\n.container_custom .home_page #main .wide .gallery .footer ul li ul li a {\n              color: black;\n              margin: 0;\n              padding: 3px 20px; }\n.container_custom .home_page #main .wide .gallery .footer ul li.dropdown {\n              position: relative; }\n.container_custom .home_page #main .wide .gallery .footer ul li.dropdown .dropdown-menu {\n                bottom: 100%;\n                top: auto;\n                min-width: 100px; }\n.container_custom .home_page #main .wide .gallery .footer ul li.dropdown .dropdown-menu:after {\n                position: absolute;\n                left: 2px;\n                content: \"\";\n                border: 5px solid transparent;\n                border-top-color: #fff; }\n.container_custom .home_page #main .wide .gallery .footer ul li:hover a {\n              background: none; }\n.container_custom .home_page #main .wide .gallery .footer ul li:hover ul {\n              display: block; }\n.container_custom .home_page #main .wide .gallery .footer .copy {\n              padding: 15px 0;\n              text-transform: capitalize; }\n.container_custom .home_page #main .wide .innerpage {\n          overflow: auto; }\n.container_custom .home_page #main .wide .innerpage h4 {\n            text-overflow: ellipsis;\n            overflow: hidden;\n            white-space: nowrap;\n            width: 90%; }\n#back2Top {\n  padding: 8px;\n  cursor: pointer;\n  background: #55493f;\n  font-weight: bold;\n  color: #fff;\n  position: fixed;\n  bottom: 7px;\n  right: 23px;\n  display: none; }\n.hr2 {\n  border: none;\n  border-top: medium double grey;\n  color: grey;\n  overflow: visible;\n  text-align: center; }\n.hr2 .hr2:after {\n    background: white;\n    content: \"\\0025C6\";\n    display: inline-block;\n    font-size: 1.7em;\n    position: relative;\n    top: -.75em; }\n#result {\n  margin-bottom: 10px; }\n.red {\n  color: red; }\n.title {\n  margin-bottom: 30px;\n  text-transform: capitalize;\n  text-align: center; }\n.title span {\n    display: inline-block;\n    position: relative; }\n.title span:after {\n    overflow: visible;\n    /* For IE */\n    padding: 0;\n    position: absolute;\n    content: \"\";\n    left: 0;\n    border: none;\n    border-top: medium double #333;\n    color: #333;\n    text-align: center;\n    bottom: -10px;\n    width: 100%; }\n"

/***/ }),

/***/ "./node_modules/style-loader/lib/addStyles.js":
/***/ (function(module, exports, __webpack_require__) {

/*
	MIT License http://www.opensource.org/licenses/mit-license.php
	Author Tobias Koppers @sokra
*/

var stylesInDom = {};

var	memoize = function (fn) {
	var memo;

	return function () {
		if (typeof memo === "undefined") memo = fn.apply(this, arguments);
		return memo;
	};
};

var isOldIE = memoize(function () {
	// Test for IE <= 9 as proposed by Browserhacks
	// @see http://browserhacks.com/#hack-e71d8692f65334173fee715c222cb805
	// Tests for existence of standard globals is to allow style-loader
	// to operate correctly into non-standard environments
	// @see https://github.com/webpack-contrib/style-loader/issues/177
	return window && document && document.all && !window.atob;
});

var getElement = (function (fn) {
	var memo = {};

	return function(selector) {
		if (typeof memo[selector] === "undefined") {
			var styleTarget = fn.call(this, selector);
			// Special case to return head of iframe instead of iframe itself
			if (styleTarget instanceof window.HTMLIFrameElement) {
				try {
					// This will throw an exception if access to iframe is blocked
					// due to cross-origin restrictions
					styleTarget = styleTarget.contentDocument.head;
				} catch(e) {
					styleTarget = null;
				}
			}
			memo[selector] = styleTarget;
		}
		return memo[selector]
	};
})(function (target) {
	return document.querySelector(target)
});

var singleton = null;
var	singletonCounter = 0;
var	stylesInsertedAtTop = [];

var	fixUrls = __webpack_require__("./node_modules/style-loader/lib/urls.js");

module.exports = function(list, options) {
	if (typeof DEBUG !== "undefined" && DEBUG) {
		if (typeof document !== "object") throw new Error("The style-loader cannot be used in a non-browser environment");
	}

	options = options || {};

	options.attrs = typeof options.attrs === "object" ? options.attrs : {};

	// Force single-tag solution on IE6-9, which has a hard limit on the # of <style>
	// tags it will allow on a page
	if (!options.singleton && typeof options.singleton !== "boolean") options.singleton = isOldIE();

	// By default, add <style> tags to the <head> element
	if (!options.insertInto) options.insertInto = "head";

	// By default, add <style> tags to the bottom of the target
	if (!options.insertAt) options.insertAt = "bottom";

	var styles = listToStyles(list, options);

	addStylesToDom(styles, options);

	return function update (newList) {
		var mayRemove = [];

		for (var i = 0; i < styles.length; i++) {
			var item = styles[i];
			var domStyle = stylesInDom[item.id];

			domStyle.refs--;
			mayRemove.push(domStyle);
		}

		if(newList) {
			var newStyles = listToStyles(newList, options);
			addStylesToDom(newStyles, options);
		}

		for (var i = 0; i < mayRemove.length; i++) {
			var domStyle = mayRemove[i];

			if(domStyle.refs === 0) {
				for (var j = 0; j < domStyle.parts.length; j++) domStyle.parts[j]();

				delete stylesInDom[domStyle.id];
			}
		}
	};
};

function addStylesToDom (styles, options) {
	for (var i = 0; i < styles.length; i++) {
		var item = styles[i];
		var domStyle = stylesInDom[item.id];

		if(domStyle) {
			domStyle.refs++;

			for(var j = 0; j < domStyle.parts.length; j++) {
				domStyle.parts[j](item.parts[j]);
			}

			for(; j < item.parts.length; j++) {
				domStyle.parts.push(addStyle(item.parts[j], options));
			}
		} else {
			var parts = [];

			for(var j = 0; j < item.parts.length; j++) {
				parts.push(addStyle(item.parts[j], options));
			}

			stylesInDom[item.id] = {id: item.id, refs: 1, parts: parts};
		}
	}
}

function listToStyles (list, options) {
	var styles = [];
	var newStyles = {};

	for (var i = 0; i < list.length; i++) {
		var item = list[i];
		var id = options.base ? item[0] + options.base : item[0];
		var css = item[1];
		var media = item[2];
		var sourceMap = item[3];
		var part = {css: css, media: media, sourceMap: sourceMap};

		if(!newStyles[id]) styles.push(newStyles[id] = {id: id, parts: [part]});
		else newStyles[id].parts.push(part);
	}

	return styles;
}

function insertStyleElement (options, style) {
	var target = getElement(options.insertInto)

	if (!target) {
		throw new Error("Couldn't find a style target. This probably means that the value for the 'insertInto' parameter is invalid.");
	}

	var lastStyleElementInsertedAtTop = stylesInsertedAtTop[stylesInsertedAtTop.length - 1];

	if (options.insertAt === "top") {
		if (!lastStyleElementInsertedAtTop) {
			target.insertBefore(style, target.firstChild);
		} else if (lastStyleElementInsertedAtTop.nextSibling) {
			target.insertBefore(style, lastStyleElementInsertedAtTop.nextSibling);
		} else {
			target.appendChild(style);
		}
		stylesInsertedAtTop.push(style);
	} else if (options.insertAt === "bottom") {
		target.appendChild(style);
	} else if (typeof options.insertAt === "object" && options.insertAt.before) {
		var nextSibling = getElement(options.insertInto + " " + options.insertAt.before);
		target.insertBefore(style, nextSibling);
	} else {
		throw new Error("[Style Loader]\n\n Invalid value for parameter 'insertAt' ('options.insertAt') found.\n Must be 'top', 'bottom', or Object.\n (https://github.com/webpack-contrib/style-loader#insertat)\n");
	}
}

function removeStyleElement (style) {
	if (style.parentNode === null) return false;
	style.parentNode.removeChild(style);

	var idx = stylesInsertedAtTop.indexOf(style);
	if(idx >= 0) {
		stylesInsertedAtTop.splice(idx, 1);
	}
}

function createStyleElement (options) {
	var style = document.createElement("style");

	options.attrs.type = "text/css";

	addAttrs(style, options.attrs);
	insertStyleElement(options, style);

	return style;
}

function createLinkElement (options) {
	var link = document.createElement("link");

	options.attrs.type = "text/css";
	options.attrs.rel = "stylesheet";

	addAttrs(link, options.attrs);
	insertStyleElement(options, link);

	return link;
}

function addAttrs (el, attrs) {
	Object.keys(attrs).forEach(function (key) {
		el.setAttribute(key, attrs[key]);
	});
}

function addStyle (obj, options) {
	var style, update, remove, result;

	// If a transform function was defined, run it on the css
	if (options.transform && obj.css) {
	    result = options.transform(obj.css);

	    if (result) {
	    	// If transform returns a value, use that instead of the original css.
	    	// This allows running runtime transformations on the css.
	    	obj.css = result;
	    } else {
	    	// If the transform function returns a falsy value, don't add this css.
	    	// This allows conditional loading of css
	    	return function() {
	    		// noop
	    	};
	    }
	}

	if (options.singleton) {
		var styleIndex = singletonCounter++;

		style = singleton || (singleton = createStyleElement(options));

		update = applyToSingletonTag.bind(null, style, styleIndex, false);
		remove = applyToSingletonTag.bind(null, style, styleIndex, true);

	} else if (
		obj.sourceMap &&
		typeof URL === "function" &&
		typeof URL.createObjectURL === "function" &&
		typeof URL.revokeObjectURL === "function" &&
		typeof Blob === "function" &&
		typeof btoa === "function"
	) {
		style = createLinkElement(options);
		update = updateLink.bind(null, style, options);
		remove = function () {
			removeStyleElement(style);

			if(style.href) URL.revokeObjectURL(style.href);
		};
	} else {
		style = createStyleElement(options);
		update = applyToTag.bind(null, style);
		remove = function () {
			removeStyleElement(style);
		};
	}

	update(obj);

	return function updateStyle (newObj) {
		if (newObj) {
			if (
				newObj.css === obj.css &&
				newObj.media === obj.media &&
				newObj.sourceMap === obj.sourceMap
			) {
				return;
			}

			update(obj = newObj);
		} else {
			remove();
		}
	};
}

var replaceText = (function () {
	var textStore = [];

	return function (index, replacement) {
		textStore[index] = replacement;

		return textStore.filter(Boolean).join('\n');
	};
})();

function applyToSingletonTag (style, index, remove, obj) {
	var css = remove ? "" : obj.css;

	if (style.styleSheet) {
		style.styleSheet.cssText = replaceText(index, css);
	} else {
		var cssNode = document.createTextNode(css);
		var childNodes = style.childNodes;

		if (childNodes[index]) style.removeChild(childNodes[index]);

		if (childNodes.length) {
			style.insertBefore(cssNode, childNodes[index]);
		} else {
			style.appendChild(cssNode);
		}
	}
}

function applyToTag (style, obj) {
	var css = obj.css;
	var media = obj.media;

	if(media) {
		style.setAttribute("media", media)
	}

	if(style.styleSheet) {
		style.styleSheet.cssText = css;
	} else {
		while(style.firstChild) {
			style.removeChild(style.firstChild);
		}

		style.appendChild(document.createTextNode(css));
	}
}

function updateLink (link, options, obj) {
	var css = obj.css;
	var sourceMap = obj.sourceMap;

	/*
		If convertToAbsoluteUrls isn't defined, but sourcemaps are enabled
		and there is no publicPath defined then lets turn convertToAbsoluteUrls
		on by default.  Otherwise default to the convertToAbsoluteUrls option
		directly
	*/
	var autoFixUrls = options.convertToAbsoluteUrls === undefined && sourceMap;

	if (options.convertToAbsoluteUrls || autoFixUrls) {
		css = fixUrls(css);
	}

	if (sourceMap) {
		// http://stackoverflow.com/a/26603875
		css += "\n/*# sourceMappingURL=data:application/json;base64," + btoa(unescape(encodeURIComponent(JSON.stringify(sourceMap)))) + " */";
	}

	var blob = new Blob([css], { type: "text/css" });

	var oldSrc = link.href;

	link.href = URL.createObjectURL(blob);

	if(oldSrc) URL.revokeObjectURL(oldSrc);
}


/***/ }),

/***/ "./node_modules/style-loader/lib/urls.js":
/***/ (function(module, exports) {


/**
 * When source maps are enabled, `style-loader` uses a link element with a data-uri to
 * embed the css on the page. This breaks all relative urls because now they are relative to a
 * bundle instead of the current page.
 *
 * One solution is to only use full urls, but that may be impossible.
 *
 * Instead, this function "fixes" the relative urls to be absolute according to the current page location.
 *
 * A rudimentary test suite is located at `test/fixUrls.js` and can be run via the `npm test` command.
 *
 */

module.exports = function (css) {
  // get current location
  var location = typeof window !== "undefined" && window.location;

  if (!location) {
    throw new Error("fixUrls requires window.location");
  }

	// blank or null?
	if (!css || typeof css !== "string") {
	  return css;
  }

  var baseUrl = location.protocol + "//" + location.host;
  var currentDir = baseUrl + location.pathname.replace(/\/[^\/]*$/, "/");

	// convert each url(...)
	/*
	This regular expression is just a way to recursively match brackets within
	a string.

	 /url\s*\(  = Match on the word "url" with any whitespace after it and then a parens
	   (  = Start a capturing group
	     (?:  = Start a non-capturing group
	         [^)(]  = Match anything that isn't a parentheses
	         |  = OR
	         \(  = Match a start parentheses
	             (?:  = Start another non-capturing groups
	                 [^)(]+  = Match anything that isn't a parentheses
	                 |  = OR
	                 \(  = Match a start parentheses
	                     [^)(]*  = Match anything that isn't a parentheses
	                 \)  = Match a end parentheses
	             )  = End Group
              *\) = Match anything and then a close parens
          )  = Close non-capturing group
          *  = Match anything
       )  = Close capturing group
	 \)  = Match a close parens

	 /gi  = Get all matches, not the first.  Be case insensitive.
	 */
	var fixedCss = css.replace(/url\s*\(((?:[^)(]|\((?:[^)(]+|\([^)(]*\))*\))*)\)/gi, function(fullMatch, origUrl) {
		// strip quotes (if they exist)
		var unquotedOrigUrl = origUrl
			.trim()
			.replace(/^"(.*)"$/, function(o, $1){ return $1; })
			.replace(/^'(.*)'$/, function(o, $1){ return $1; });

		// already a full url? no change
		if (/^(#|data:|http:\/\/|https:\/\/|file:\/\/\/)/i.test(unquotedOrigUrl)) {
		  return fullMatch;
		}

		// convert the url to a full url
		var newUrl;

		if (unquotedOrigUrl.indexOf("//") === 0) {
		  	//TODO: should we add protocol?
			newUrl = unquotedOrigUrl;
		} else if (unquotedOrigUrl.indexOf("/") === 0) {
			// path should be relative to the base url
			newUrl = baseUrl + unquotedOrigUrl; // already starts with '/'
		} else {
			// path should be relative to current directory
			newUrl = currentDir + unquotedOrigUrl.replace(/^\.\//, ""); // Strip leading './'
		}

		// send back the fixed url(...)
		return "url(" + JSON.stringify(newUrl) + ")";
	});

	// send back the fixed css
	return fixedCss;
};


/***/ }),

/***/ "./src/assets/scss/styles.scss":
/***/ (function(module, exports, __webpack_require__) {

// style-loader: Adds some css to the DOM by adding a <style> tag

// load the styles
var content = __webpack_require__("./node_modules/raw-loader/index.js!./node_modules/postcss-loader/lib/index.js??embedded!./node_modules/sass-loader/lib/loader.js??ref--8-3!./src/assets/scss/styles.scss");
if(typeof content === 'string') content = [[module.i, content, '']];
// Prepare cssTransformation
var transform;

var options = {"hmr":true}
options.transform = transform
// add the styles to the DOM
var update = __webpack_require__("./node_modules/style-loader/lib/addStyles.js")(content, options);
if(content.locals) module.exports = content.locals;
// Hot Module Replacement
if(false) {
	// When the styles change, update the <style> tags
	if(!content.locals) {
		module.hot.accept("!!../../../node_modules/raw-loader/index.js!../../../node_modules/postcss-loader/lib/index.js??embedded!../../../node_modules/sass-loader/lib/loader.js??ref--8-3!./styles.scss", function() {
			var newContent = require("!!../../../node_modules/raw-loader/index.js!../../../node_modules/postcss-loader/lib/index.js??embedded!../../../node_modules/sass-loader/lib/loader.js??ref--8-3!./styles.scss");
			if(typeof newContent === 'string') newContent = [[module.id, newContent, '']];
			update(newContent);
		});
	}
	// When the module is disposed, remove the <style> tags
	module.hot.dispose(function() { update(); });
}

/***/ }),

/***/ 2:
/***/ (function(module, exports, __webpack_require__) {

module.exports = __webpack_require__("./src/assets/scss/styles.scss");


/***/ })

},[2]);
//# sourceMappingURL=styles.bundle.js.map